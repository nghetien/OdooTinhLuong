<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="payroll_teacher_action" model="ir.actions.act_window">
            <field name="name">PayrollTeacher</field>
            <field name="res_model">teacher</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p clas="oe_view_nocontent_create">
                    Tạo mới giáo viên ở đây!
                </p>
            </field>
        </record>

        <menuitem id="payroll_teacher_root" 
                name="Tính lương giảng viên"
                sequence="10"/>

        <menuitem id="payroll_teacher_table" 
                name="Bảng tính lương"
                action="payroll_teacher_action"
                parent="payroll_teacher_root"
                sequence="10"/>
</odoo>